/**
 * Usar un loop y que ese calcule que numeros son pares e impares
 * Crear un algoritmo que nos imprima solamente los numeros impares (pero del 0 al 10)
*/

let i = 0;

while (i<=10) {
    
    if(i % 2 !== 0)
    {
         console.log("El numero es impar", i);
    }
    i++;
}
/**
*for (i = 0; i < 10; i++)
{
    if(i%2 !== 0)
    {
        console.log("El numero es impar", i);
    }

} 
 * 
 * 
 * 
 */
